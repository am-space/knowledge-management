# Knowledge.Server.ArchitectureTests

Tests that enforce module ownership and dependency direction once executable projects exist. Add
rules only for boundaries that are important enough to enforce mechanically.


# Scripts

This directory will contain the repository's canonical setup and verification entry points after the
application is scaffolded. CI should invoke the same scripts used locally rather than duplicate their
logic.


# Knowledge module

Owns stable knowledge-node identity, immutable revisions, semantic relations, hierarchy, history,
and the application services and transport adapters that operate on them.

Planned internal areas are `Domain`, `Features`, `Presentation/Http`, `Presentation/Mcp`, and
provider-neutral entity configuration under `Infrastructure`.


# Workspaces module

Owns workspaces, memberships, roles, workspace selection, and workspace-level authorization.
Knowledge remains owned by the Knowledge module but every tenant-owned record is scoped by a
workspace governed here.

Local mode may automatically create one personal workspace, but it retains the same workspace model
used by the server profile.

